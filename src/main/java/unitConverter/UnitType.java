package unitConverter;

/**
 * UnitType enum is a class with a fixed set of unit's types.
 * @author Kunyaruk Katebunlu
 */
public enum UnitType {
	/** enum members */
	Length,
	Temperature,
	Weight,
	Volume;
}
